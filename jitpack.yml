version: 1
jdk:
  - openjdk8  # 使用 Java 8 与旧版 Gradle 和 Android SDK 兼容

android:
  components:
    - build-tools-21.1.0
    - android-21  # 需要的 Android SDK 和 Build Tools 版本

script:
  - ./gradlew clean build  # 清理并构建项目